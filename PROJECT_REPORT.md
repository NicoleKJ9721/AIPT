# AIPT 平台开发进度报告

**更新日期**：2026-07-28
**状态**：工业视觉工作流加固与第三代泛化检测设计阶段

## 1. 当前能力

| 模块 | 当前状态 | 说明 |
| :--- | :--- | :--- |
| 项目、数据集与本地存储 | 已实现 | 项目、数据集、图片、标注和训练产物可追溯；默认与源码目录分离。 |
| 智能标注 | 已实现 | 矩形/多边形标注、模板匹配、YOLO 预标注、FastSAM/SAM 点提示分割。 |
| 检测与分割训练 | 已实现 | 可选择 YOLO 检测或分割任务；默认基线为 YOLOv8s；训练异常不会伪装成成功。 |
| 模型管理、导出与推理 | 已实现 | 支持已训练模型管理、评估/导出与本地推理流程。 |
| 多模型串联检测 | 已实现 | 支持真实后端串联推理、动态 ROI 与首节点固定工位 ROI。 |
| Windows 长路径存储 | 已加固 | 数据集、模型和导出路径在 Windows 深层目录下使用扩展路径处理。 |
| 手工数据增强 | 有意停用 | 页面仅保留数据集快照/版本固化；训练阶段采用 YOLO 在线增广，避免重复增强。 |

## 2. 本次工作流加固

### 2.1 训练与模型

- 默认模型基线调整为 `YOLOv8s`，并保留本地权重与旧权重回退逻辑。
- 模型训练支持 `detect` 与 `segment` 两类任务；矩形框可转换为分割训练所需的矩形多边形，已有多边形标注保留其轮廓。
- 非测试环境中的真实训练错误会将任务标记为失败并保留日志；模拟训练仅能由测试/显式开发开关启用。
- 固定 `ultralytics==8.4.107`，避免依赖漂移影响现有 API 与模型行为。

### 2.2 工位 ROI 与多模型流程

- 固定工位 ROI 存为首节点的归一化配置，不裁改源图。
- 推理前仅裁出检测区域，所有检测框和分割结果自动转换回原图坐标。
- 可继续衔接基于上一节点检测框或分割区域的动态 ROI，实现粗检→精检。
- 已增加单元测试：验证固定 ROI 的输入尺寸及检测框的坐标回映射。

### 2.3 边缘能力边界

- 当前没有背景抠图、matting 或前景永久删除模块。
- 现有图片裁剪/旋转是数据清洗操作，会修改源图，并在已有标注时拒绝执行；不应用作生产工位配方。
- FastSAM/SAM 当前用于交互式分割标注，不应被表述为零样本缺陷检测模型。

## 3. 第三代泛化检测：已设计，尚未接入

第三代能力应以“未知异常发现 + 少样本正常品建模”为目标，独立于监督式 YOLO 训练链路。其正式设计见 [docs/THIRD_GEN_GENERALIZATION_DETECTION_DESIGN.md](docs/THIRD_GEN_GENERALIZATION_DETECTION_DESIGN.md)。

当前结论：

- 已知缺陷：继续用 YOLO 检测/分割。
- 10–100 张正常品：优先评估 PatchCore；低延迟约束下评估 EfficientAD。
- 零正常品：WinCLIP / AnomalyCLIP 仅作为异常候选发现，首版必须进入人工复核，不可直接作为自动放行/拦截依据。
- 最新视觉基础模型异常方案可作为对比基准，但经安全审计、权重锁定、工位真实数据验证前不得设为生产默认。

## 4. 验收与测试状态

- 已完成：差异检查、Python 静态编译、固定 ROI 坐标映射测试用例。
- 待完成：在 Windows + Python 3.11 + 项目完整依赖环境中执行完整后端测试、前端 lint/build 与固定 ROI 端到端回归。
- 第三代模型验收必须采用按日期、相机、光照和产品批次划分的工位真实数据；不能仅使用随机切分得出生产结论。
- 研究数据集只能作为模型筛选基准，不能替代现场验收或直接作为商业交付数据。

## 5. 后续优先级

1. 将固定 ROI 扩展为可版本化的“工位配方”：拖拽选区、忽略区、多边形掩膜、定位点和光照质量门控。
2. 以独立异常检测服务接入 PatchCore / EfficientAD，避免与 YOLO 训练运行环境和模型产物耦合。
3. 增加零样本候选发现模式与复核队列，记录热图、分数、阈值、操作者决策和配方版本。
4. 最后提供 YOLO 与异常模型融合部署：已知缺陷命中、未知异常命中和“待复核”三种明确结果。
