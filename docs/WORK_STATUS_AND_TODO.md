# AIPT 远程调试工作状态与待办

**更新日期**：2026-07-28

**工作分支**：`codex/aipt-workflow-fixes`

**功能基线提交**：`fe824a8`

## 状态说明

- `已完成并验证`：功能已经执行过相应测试或真实流程验证。
- `已实现，待复验`：代码已经完成并通过静态检查，但当前版本尚未在目标 Windows 环境完成完整回归。
- `尚未实施`：仍处于设计、选型或等待外部条件阶段。

## 1. 已完成并验证

### 1.1 项目理解与功能审计

- [x] 梳理 AIPT 的核心目标和项目结构。
- [x] 审计项目、数据集、标注、数据快照、训练、推理和多模型流程的代码链路。
- [x] 确认当前智能标注包含模板匹配、YOLO 预标注和 FastSAM/SAM 类点提示分割。
- [x] 确认当前代码没有 SAM3 实际接入，也没有背景抠图、matting 或前景永久删除模块。
- [x] 确认现有图片裁剪会修改源图，且已有标注时拒绝裁剪，不适合作为生产工位 ROI。
- [x] 确认多模型流程已有基于上一节点检测框/分割区域的动态 ROI。

### 1.2 Windows 云端历史基线验证

以下结果来自固定工位 ROI 等最新本地改动之前的云端版本，用于证明原有主流程曾经可运行，不代表当前分支已经完成最终回归。

- [x] 在 Windows + NVIDIA GPU 环境部署并启动 AIPT。
- [x] 后端测试历史基线：`77 passed`。
- [x] 前端历史基线：lint 和 build 通过。
- [x] 真实 API 流程完成项目、数据集、图片/标注等基本联调。
- [x] FastSAM 在真实图片上完成过一次点提示分割。
- [x] YOLO 完成过一次真实的单 epoch 训练，而不是模拟训练。

### 1.3 当前分支已完成的代码修改

- [x] 修复 Windows 深层数据集、模型和导出目录的长路径处理。
- [x] 将默认检测基线调整为 `YOLOv8s`，保留旧权重回退。
- [x] 将 `ultralytics` 固定为 `8.4.107`，减少依赖漂移。
- [x] 训练任务支持 `detect` 和 `segment`。
- [x] 检测/多边形标注可转换为对应的 YOLO 检测或分割训练标签。
- [x] 真实训练异常会进入失败状态并保留日志，不再静默回退成“模拟成功”。
- [x] 多模型流程增加固定工位 ROI：
  - 不修改原始图片；
  - 使用归一化坐标，兼容不同分辨率；
  - 只允许在首节点配置；
  - 检测结果自动映射回原图坐标；
  - 动态 ROI 为空时回退到配置的输入区域，而不是未经处理的全图。
- [x] 前端增加固定 ROI 开关、百分比参数、预览叠加和状态标识。
- [x] 增加固定 ROI 输入尺寸与坐标回映射测试用例。
- [x] 当前分支通过 `git diff --check` 和相关 Python 文件静态编译。

### 1.4 文档与 GitHub

- [x] 更新 `README.md`，纠正“多模型流程仅有 UI”等过时说明。
- [x] 更新 `PROJECT_REPORT.md`，区分已实现、待验证和规划中的能力。
- [x] 更新本地存储文档中的默认权重策略。
- [x] 新增 `docs/THIRD_GEN_GENERALIZATION_DETECTION_DESIGN.md`。
- [x] 功能代码和上述文档已推送到 GitHub 分支 `codex/aipt-workflow-fixes`。

## 2. 已实现，但尚未完成最终复验

| 项目 | 当前状态 | 未完成原因 |
| :--- | :--- | :--- |
| 当前分支完整后端测试 | 待复验 | 本机只有 Python 3.9 且缺少项目完整依赖；测试在导入 `sqlalchemy` 时停止收集。项目目标环境要求 Python 3.11。 |
| 当前分支前端 lint/build | 待复验 | 本机 `frontend/node_modules` 未安装，当前无法调用 ESLint/Vite。 |
| 固定工位 ROI 端到端运行 | 待复验 | 单元测试已经编写，静态编译通过；目标 Windows 云实例当前未运行。 |
| 默认 YOLOv8s 与分割训练的云端复验 | 待复验 | 相关本地改动完成后，云实例已经停止，尚未同步并执行新一轮训练。 |
| Windows 长路径真实数据压力测试 | 待复验 | 已有针对路径转换的测试代码，但仍需在 Windows 深层目录和大量文件下验证。 |
| 当前提交的全流程回归 | 待复验 | 历史版本曾验证过，但数据集→标注→训练→推理→串联流程尚未按当前提交重新跑完。 |

## 3. 尚未实施及原因

### 3.1 SAM3 智能标注

- [ ] 尚未下载、安装和接入 SAM3。
- [ ] 尚未验证 ModelScope SAM3 权重、依赖、显存占用、许可证和 Windows 兼容性。
- [ ] 尚未完成 SAM3 与当前点提示分割 API、前端交互和标注格式的适配。

**原因**：目标 Windows 实例当前不可用；项目约定新增依赖前需要确认；SAM3 还需要独立的权重和性能验证。当前可运行的智能分割仍是 FastSAM/SAM 类接口及 flood 回退，不应表述为 SAM3。

### 3.2 第三代零样本/少样本异常检测

- [ ] 尚未接入 PatchCore、EfficientAD、WinCLIP 或 AnomalyCLIP。
- [ ] 尚未建立异常模型独立训练/推理服务。
- [ ] 尚未实现正常品参考库、异常热图、阈值校准和复核队列。
- [ ] 尚未实现 YOLO 已知缺陷与异常模型的融合判定。

**原因**：目前完成的是架构和产品设计；需要新增异常检测依赖与模型产物类型，并在 GPU 环境中进行公平基准测试。零样本模式还必须先定义“候选发现”而非自动放行的业务边界。

### 3.3 开源工业缺陷数据集与模型对比

- [ ] 尚未下载并整理中小型工业缺陷数据集。
- [ ] 尚未完成统一训练/验证切分、模型训练、精度/延迟/显存对比。
- [ ] 尚未用真实工位数据验证跨日期、相机、光照和批次泛化。

**原因**：云实例停止后没有持续可用的 GPU 环境；公开数据集的许可证和评估协议也需要逐一固化。公开数据集只能用于筛选模型，不能代替现场验收。

### 3.4 背景剔除与高级关键区域

- [ ] 尚未实现背景剔除或前景掩膜模块。
- [ ] 固定 ROI 尚未提供在预览图上直接拖拽绘制的交互。
- [ ] 尚未实现多边形检测区、忽略区、夹具/反光排除区。
- [ ] 尚未实现模板/特征点/标记物定位和 ROI 自动对齐。
- [ ] 尚未将 ROI、掩膜、对齐和图像质量门控升级为可版本化“工位配方”。

**原因**：本轮先完成了不改原图、坐标可追溯的固定 ROI 基础能力。背景与关键区域需要统一的数据模型，否则容易出现训练和推理预处理不一致。

### 3.5 GitHub 合并

- [ ] 当前工作分支尚未合并到默认分支。

**原因**：已经推送工作分支，但尚未创建/合并 Pull Request；应在目标环境回归通过后再合并。

## 4. 待办清单

### P0：恢复环境并完成当前分支验收

- [ ] 处理云实例不可用问题并启动 Windows 主机。
- [ ] 在云端拉取 `codex/aipt-workflow-fixes` 最新提交。
- [ ] 核对 Python 3.11、CUDA、PyTorch、Ultralytics 和前端依赖。
- [ ] 执行完整后端测试并保存测试结果。
- [ ] 执行前端 lint 和 build。
- [ ] 逐步回归项目→数据集→标注→快照→检测训练→分割训练→推理。
- [ ] 验证固定 ROI 的保存、重新加载、预览、推理和坐标回映射。
- [ ] 使用 Windows 深层目录验证上传、克隆、训练产物、导出和删除。
- [ ] 修复回归中发现的问题，并更新本文件中的真实结果。

### P1：建立可生产使用的工位配方

- [ ] 设计 `StationProfile/InspectionRecipe` 数据模型。
- [ ] 将固定 ROI 从管线首节点配置升级为可复用工位配置。
- [ ] 增加拖拽绘制固定 ROI。
- [ ] 增加多边形检测区和忽略区。
- [ ] 增加工件定位/对齐和清晰度、曝光、光照质量门控。
- [ ] 保证原图不可变，并记录所有预处理和配方版本。
- [ ] 为工位配方增加保存、复制、回滚和测试。

### P1：接入少样本正常品异常检测

- [ ] 确认是否允许新增 Anomalib 等依赖，并确定独立环境/服务方式。
- [ ] 首先接入 PatchCore 作为准确率基线。
- [ ] 接入 EfficientAD 作为低延迟候选。
- [ ] 实现正常品参考集、模型产物、热图和图像级分数存储。
- [ ] 实现阈值校准与通过/异常/待复核三态结果。
- [ ] 用同一数据切分比较误报、漏检、像素定位、延迟和显存。

### P2：零样本探索与智能标注升级

- [ ] 评估 WinCLIP、AnomalyCLIP 和更新的视觉基础模型异常方案。
- [ ] 零样本首版只进入候选发现和人工复核流程。
- [ ] 下载并审计 SAM3 代码、权重和许可证。
- [ ] 将 SAM3 作为标注/工位掩膜生成助手接入，而不是异常分类器。
- [ ] 对比 SAM3、现有 FastSAM 和人工标注的精度、速度与显存。
- [ ] 将复核结果回流到主动学习和 YOLO 再训练流程。

### P2：发布与文档

- [ ] 完成当前分支回归后创建 Pull Request。
- [ ] 在 PR 中记录测试环境、测试结果、已知限制和迁移影响。
- [ ] 合并后更新默认分支部署说明和版本记录。
- [ ] 为第三代能力补充用户手册、验收模板和模型选择报告。

## 5. 当前外部阻塞

1. Windows 云实例当前处于不可运行状态，需要先恢复实例。
2. SAM3、Anomalib 等新增依赖尚未获得针对具体依赖的安装确认。
3. 尚缺少用于最终生产验收的真实工位正常品、已知缺陷和未知缺陷数据。
4. 默认分支是否合并，需要在当前分支完整回归后决定。
